---
sidebar_position: 6
---

import CalculateCheckDigitPlayground from "../../../src/components/LinearBarcode/CalculateCheckDigitPlayground";

# チェックデジット

最後にチェックデジットを見てみましょう。

チェックデジットは、1桁目から12桁目までの数字から計算することができます。このようにして求めた数がチェックデジットと一致していれば、バーコードを正確に読み取れたことを確認できます。

それでは、具体的な計算方法を見てみましょう。

最初の12桁の数字を右側から数えた時に、奇数番目の数字には3を偶数番目の数字には1をかけて、それらを足した合計を計算します。たとえば、最初の12桁が「456995111617」である場合は、$4 \times 1 + 5 \times 3 + 6 \times 1 + 9 \times 3 + 9 \times 1 + 5 \times 3 + 1 \times 1 + 1 \times 3 + 1 \times 1 + 6 \times 3 + 1 \times 1 + 7 \times 3 = 121$になります。

次に、この結果の最後の桁を取り出します。先ほどの例だと、「1」ですね。

最後に、この値を10から引きます。つまり、$10 - 1 = 9$がチェックデジットとなります。(もしも、これが0である場合、$10 - 0 = 10$となりますが、この場合は「0」がチェックデジットとなります。)

12桁の数字を入力して、チェックデジットが何になるか確認してみてください。

<CalculateCheckDigitPlayground />
